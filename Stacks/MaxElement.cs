namespace HackerRank.Stacks;

public class MaxElement
{

    public static List<int> Solve(List<string> operations)
    {
        var maxes = new List<int>();
        foreach (var op in operations)
        {
            var commands = op.Split(' ');
            if (commands[0] == "1")
            {
                
            }else if (commands[1] == "2")
            {
                
            }
            else
            {
                
            }
        }
        return maxes;
    }
}